// muve — shared UI constants. Loaded before app.js.

// App version, shown in the sidebar under the app name.
const APP_VERSION = "1.0.0";

// Show/hide the Zones vs Stereo L/R vibration-routing toggle in the UI.
// "zones" is always the default vibration mode regardless of this flag.
const SHOW_VIBRATION_MODE_SWITCH = false;
